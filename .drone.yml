---
kind: pipeline
type: docker
name: alpine

trigger:
  ref:
    - refs/heads/master
    - refs/pull/*/head
    - refs/tags/*

platform:
  os: linux
  arch: amd64

steps:
  - name: build
    image: alpine:edge
    commands:
      - ls
      - echo "Hello world" > echo.txt
      - ls
      - exit 0
  
  - name: list
    image: alpine:edge
    commands:
      - ls
    depends_on: ['build']
   
  - name: publish
    image: plugins/github-release
    settings:
      api_key:
        from_secret: github_token
    files: /tmp/alpinebuildcache/boot/*
    checksum:
      - md5
      - sha1
    when:
      event: [tag]
    depends_on: ['list']
  
  - name: notify
    image: appleboy/drone-telegram
    settings:
      token:
        from_secret: telegram_token
      to: 205514323
    when:
      status: [ success, failure ]
      event: [tag]
    depends_on: ['publish']
